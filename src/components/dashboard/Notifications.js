import React from "react";
import firebase from "../../config/firebase";

const Notifications = () => {
  return (
    <div>
      <p>Notifications</p>
    </div>
  );
};

export default Notifications;
